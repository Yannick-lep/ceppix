<h2>{{ title }}</h2>

<h3>{{ pokemon.name }}</h3>

<img src="{{ pokemon.image }}" alt="{{ pokemon.name }}">

<ul>
    <li>Taille : {{ pokemon.height }}</li>
    <li>Poids : {{ pokemon.weight }}</li>
</ul>
